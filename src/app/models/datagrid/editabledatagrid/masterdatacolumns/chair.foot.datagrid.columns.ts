import { DatagridColumn } from '../../datagrid.column';

export class ChairfootDatagridColumns {
  partNumber: DatagridColumn;

  constructor(partNumber: DatagridColumn) {
    this.partNumber = partNumber;
  }
}
